build/
.backup/

# Scratch dir `make test` wipes and recreates for the file-backed store tests.
tests/tmp/

# --- tools/suspend-writer ---
# Build outputs (host + ARM binaries, moc, rendered previews).
tools/suspend-writer/build/
# Local sample roster/month JSON (carries real habit names).
tools/suspend-writer/sample-data/
# The reMarkable SDK (huge, user-installed locally) and its installers.
tools/suspend-writer/sdk/
tools/suspend-writer/*-toolchain.sh
